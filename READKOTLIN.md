# 🎶 KOTLIN STUDY

## 1. [문자열](https://jaejong.tistory.com/48)

* 복잡한 문자열을 표현시, 문자열 ("") 내부에 $(달러기호)를 사용해 변수ㆍ식을 넣을 수 있움
* 큰따옴표(") 3개를 리터럴로 사용해 여러줄 문자열 그대로 표현 가능

> println("나는 ${str} 입니다.")  

## 2. [입력과 출력](https://slowsure.tistory.com/84)

* readLine() : String? 타입으로 입력을 받는다. 그렇기에 반드시 다른 type으로 변환할 때에는 !!를 써줘야 한다.
* next() : 스캐너를 이용해 next를 입력하면 String 타입으로 문자 혹은 문자열을 입력 받는다. 공백 혹은 개행문자 기준으로 입력 받는다.
* nextLine() : String 타입으로 문자 혹은 문자열을 입력받는다.

